server:
  port : 88889

spring:
  application:
    name: auth-service

  config:
    import: "optional:configserver:http://localhost:8888/"

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    # 🚨 DB 연결 정보 명시
    url: jdbc:h2:mem:authdb  # 'authdb'로 이름을 지정하는 것을 추천합니다.
    username: sa
    password:
    driver-class-name: org.h2.Driver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

service:
  jwt:
    expiration: 86400000
    # 👇 아래의 Base64 전용 키로 교체하세요 (밑줄 없음)
    secret-key: "bXlfc3VwZXJfc2VjcmV0X2tleV9oZWVkYWU5M19tc2FfcHJvamVjdF8xMjM0X2VuY29kZWQ="